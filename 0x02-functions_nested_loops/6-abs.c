#include "main.h"
/**
 * _abs - calculate the absolute value of an integer.
 *
 * @i:enter number as integer.
 *
 * Return: absolute value
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (0);
	}
	else
	{
		return (i * -1);
	}
}
